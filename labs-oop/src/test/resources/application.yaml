spring:
  datasource:
    url: jdbc:tc:postgresql:16:///labsdb?TC_DAEMON=true
    username: labber
    password: labber
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
